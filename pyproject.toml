[project]
name = "agent_evals"
version = "0.1.0"
description = "Agent evaluation API"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]  # Tell Hatch to include the app/ directory
